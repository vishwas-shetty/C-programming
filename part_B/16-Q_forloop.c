#include <stdio.h>

// Q) Print the number from 0 to 10?
int main()
{
    for (int i = 0; i <= 10; i = i + 1) // we can write i++ for only i = i+1
    {
        printf("%d \n", i);
    }

    return 0;
}
